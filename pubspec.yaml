name: test1
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # 已更新以更好地兼容新版依赖包

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1 # 添加这一行 (版本号可能更新，以最新为准)
  flutter_advanced_segment: ^3.0.2
  
  
  # 请添加下面这两行
  go_router: ^14.1.0
  provider: ^6.1.2
  photo_view: ^0.15.0 
  intl: ^0.19.0
  shared_preferences: ^2.2.3 # 用于本地存储
  dio: ^5.4.3+1             # 用于网络请求
  pretty_dio_logger: ^1.3.1 # 
  cached_network_image: ^3.3.1
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # 建议更新到较新版本

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/svgs/
    #- assets/images/2.0x/ # 声明2.0x文件夹
    #- assets/images/3.0x/ # 声明3.0x文件夹